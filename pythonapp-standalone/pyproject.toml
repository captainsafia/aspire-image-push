[project]
name = "pythonapp-standalone"
version = "0.1.0"
description = "Standalone FastAPI application for Aspire"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]
